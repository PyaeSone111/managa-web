/**
 * Installed app version baked into this build.
 * Bump on every release — keep in sync with android/app/build.gradle (versionName, versionCode).
 */
export const LOCAL_APP_VERSION = '2.1.018';
export const LOCAL_APP_VERSION_CODE = 2;
