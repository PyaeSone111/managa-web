; PHP limits for uploads (Hostinger reads this from document root)
; 503 on POST /api/v1/admin/upload/bulk often means these were too low.
upload_max_filesize = 64M
post_max_size = 128M
max_execution_time = 120
memory_limit = 256M
